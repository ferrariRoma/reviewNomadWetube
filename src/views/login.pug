extends base

block content
    if error
        h1=error
    form(method="POST")
        input(name="email",placeholder="email", type="text" required, maxlength=20)
        input(name="password",placeholder="password", type="password", required, maxlength=30, minlength=8)
        button Login